(function($) {
    $(document).ready(function() {
        var loginLink = $('.login-link', '#menu');

        loginLink.on('click', function(e) {
            e.preventDefault();
            $( loginLink.children('a').attr('href') ).modal('toggle');
        })
    });
})(jQuery);